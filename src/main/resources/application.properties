spring.application.name=Back

spring.datasource.url=jdbc:mysql://localhost:3306/aioT?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=wngud2002@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MySQL ?? ?? (JSON ?? ?? ?? ?? ??)
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ==============================================================
# 3. SERVER CONFIGURATION
# ==============================================================
# ?? ?? (?? 8080)
#server.port=8080
# ?? ?? ?? (?? 30? -> ? ?? ?? ??, ?: 1800)
#server.servlet.session.timeout=30m

# ==============================================================
# 4. MOBIUS CONFIGURATION (Custom)
# ==============================================================
# ??? Java ???? @Value("${mobius.url}")? ??? ? ??????.
# Mobius ??? ?? (???? ?? IP)
#mobius.url=http://127.0.0.1:7579
# Mobius ??? AE ??
#mobius.ae-name=Face_Analysis_Service

# ==========================================
# ngrok ??? ?? ?? ?? ?? (SameSite)
# ==========================================
# 1. ??? ?? ???(???-??? ?)??? ? ? ?? ??
server.servlet.session.cookie.same-site=None

# 2. HTTPS ??(ngrok)??? ??? ????? ?? (SameSite=None ??? ??)
server.servlet.session.cookie.secure=true

# 3. ??????? ??? ? ???? ?? (?? ??)
server.servlet.session.cookie.http-only=true
